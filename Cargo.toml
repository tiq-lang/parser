[package]
name = "parser"
version = "0.1.0"
edition = "2024"

[dependencies]
parser-core = { path = "parser-core" }
parser-api = { path = "parser-api" }

[workspace]
members = [
    "parser-core",
    "parser-api",
]
